#define _CRT_SECURE_NO_WARNINGS
#include<stdio.h>
#include<string.h>

#define MAX_LEN 51
/*int main()
{
	char input[MAX_LEN];
	char output[MAX_LEN];
	int len, num, encoded_len = 0;
    int i;
    i = 0;

	scanf("%s", input);
    len = strlen(input);


	////////////////////////////////////////
    for (i = 0 ; i <MAX_LEN; < i ++)
    {
        if(input[i] = 'A')
        encode_len++;
    }
    



	////////////////////////////////////////

	printf("%s\n", input);
	printf("%s\n", output);

	printf("%.6lf(%d/%d)\n", (double)len / encoded_len, len, encoded_len);
	return 0;
}*/
int main(void)
{
    printf("%d" ,'Z');
}
